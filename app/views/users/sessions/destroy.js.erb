console.log('sessions.destroy.js.erb');
$('.notice').remove();

$('#micromenu_placeholder').html('<%= j render '/shared/micromenu' %>');

<% if flash[:notice] %>
    notif({
        type: "info",
        msg: "#{flash[:notice]}",
        width: "all",
        height: 100,
        position: "center"
    });
<% end %>